requests>=2.31.0
tqdm>=4.65.0

# Data processing & parquet
pandas>=2.2.0
pyarrow>=11.0.0
fastparquet>=2023.11.0

# ML / NLP
scikit-learn>=1.2.2
joblib>=1.2.0
sentencepiece>=0.1.99
sentence-transformers>=2.2.2
transformers>=4.30.0
bertopic==0.17.4
classla==2.2.1
gensim==4.4.0

# PyTorch (CUDA 12.4 wheels). If you need CPU-only, install torch from the default index without the --index-url.
--index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# Optional / helpful
python-dotenv>=1.0.0
colorama>=0.4.6

# Notes:
# - If you don't have an NVIDIA GPU or want a different CUDA version, remove the --index-url line and install
#   a compatible `torch` wheel manually: https://pytorch.org/get-started/locally/
requests
tqdm

# Use the specific NVIDIA GPU index for Windows
--index-url https://download.pytorch.org/whl/cu124

# Core AI & GPU Support (Security Hardened v2.6.0)
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# NLP & Topic Modeling
bertopic
sentence-transformers
transformers
classla

# Data Processing & Parquet Support
pandas
fastparquet
pyarrow

# Evaluation Metrics
gensim